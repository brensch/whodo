rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
		//	check user owns user_details_object
		match /user_details/{userId} {
      allow read, update, delete: if request.auth != null && request.auth.uid == userId;
      allow create: if request.auth != null;
    }
    match /stories {
      allow read: if true;
    }
    
    match /games/{gameID} {
    	allow get: if request.auth != null;
      allow list: if false;
      allow create: if request.auth != null;

			//	check if user is in the participant id list
      allow update: if request.auth.uid in resource.data.ParticipantIDs;
      
      // check if user is just adding themself to the participant list
      allow update: if request.auth != null && request.resource.data.ParticipantIDs == resource.data.ParticipantIDs.concat([request.auth.uid])
    }
  }
}